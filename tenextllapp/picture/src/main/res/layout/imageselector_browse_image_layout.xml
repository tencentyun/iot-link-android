<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black" >

    <com.yho.image.imageselectorbrowser.HackyViewPager
        android:id="@+id/vp_image_browse"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >
    </com.yho.image.imageselectorbrowser.HackyViewPager>

    <TextView
        android:id="@+id/tv_image_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:background="@drawable/imageselector_browse_tv_shape_top_img_num"
        android:gravity="center"
        android:paddingBottom="4dp"
        android:paddingTop="4dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:textColor="@android:color/white"
        android:textSize="17sp"
        android:textStyle="bold" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/common_normal_title_height_size"
        android:layout_alignParentBottom="true"
        android:background="@android:color/transparent"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/tv_image_browse_exit"
            android:layout_width="56dp"
            android:layout_height="36dp"
            android:layout_centerVertical="true"
            android:layout_marginLeft="5dp"
            android:background="@drawable/imageselector_corner_rect_agray_shape"
            android:gravity="center"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:text="退出"
            android:textColor="@color/imageselector_folder_text_selector"
            android:textSize="17sp"
            tools:text="退出" />

        <TextView
            android:id="@+id/tv_image_browse_save"
            android:layout_width="56dp"
            android:layout_height="36dp"
            android:layout_centerVertical="true"
            android:layout_marginLeft="10dp"
            android:layout_toRightOf="@id/tv_image_browse_exit"
            android:background="@drawable/imageselector_corner_rect_agray_shape"
            android:gravity="center"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:text="保存"
            android:textColor="@color/imageselector_folder_text_selector"
            android:textSize="17sp"
            tools:text="退出" />
    </RelativeLayout>

</RelativeLayout>